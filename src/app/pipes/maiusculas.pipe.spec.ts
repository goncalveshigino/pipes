import { MaiusculasPipe } from './maiusculas.pipe';

describe('MaiusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new MaiusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
